import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('CCGE App carga correctamente', (tester) async {
    // Aquí puedes agregar pruebas para validar la carga de widgets básicos.
  });
}
